import 'package:flutter/material.dart';

const kPrimaryColor = Color.fromRGBO(25, 150, 125, 1);
final kBackgroundColor = /* DateTime.now().hour > 18 || DateTime.now().hour < 6
    ? const Color.fromARGB(255, 0, 0, 0)
    :  */const Color(0XFFE5E5E5);